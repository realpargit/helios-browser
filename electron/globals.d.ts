// Build-time constant injected by Vite (see vite.config.ts).
// `true` in dev, `false` in production builds — used to tree-shake
// bench/debug surfaces out of shipped bundles.
declare const __DEV__: boolean
